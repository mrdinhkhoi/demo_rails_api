Open terminal do this: 
  $ git clone https://github.com/mrdinhkhoi/demo_rails_api.git
  $ cd demo_rails_api 
  $ bunde
  $ rake db:migrate
  $ rake db: seed
  $ rails s
open localhost:3000/talks to see the API
then open the vue_resource_demo.html to see the list talks
